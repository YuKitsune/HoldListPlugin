<UserControl x:Class="HoldPlugin.Controls.HoldSetup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HoldPlugin"
             xmlns:viewModels="clr-namespace:HoldPlugin.ViewModels"
             Background="{x:Static local:Theme.BackgroundColor}"
             d:DataContext="{d:DesignInstance Type=viewModels:HoldSetupViewModel}"
             mc:Ignorable="d"
             Height="64"
             Width="480">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/HoldPlugin;component/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            
            <Style TargetType="Button" BasedOn="{StaticResource ButtonBaseStyle}">
                <Setter Property="Width" Value="104"/>
                <Setter Property="Height" Value="24"/>
                
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Button 1 -->
        <Grid Grid.Column="0" DataContext="{Binding Button1}">
            <!-- Display mode -->
            <Button Content="{Binding DisplayText}" Command="{Binding EditCommand}">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource ButtonBaseStyle}">
                        <Setter Property="Width" Value="104"/>
                        <Setter Property="Height" Value="24"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Edit mode -->
            <TextBox Text="{Binding EditValue, UpdateSourceTrigger=PropertyChanged}"
                     Foreground="{x:Static local:Theme.InteractiveTextColor}"
                     MaxLength="11"
                     LostFocus="HoldPointTextBox_LostFocus"
                     IsVisibleChanged="HoldPointTextBox_IsVisibleChanged">
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding CommitEditCommand}"/>
                    <KeyBinding Key="Escape" Command="{Binding CancelEditCommand}"/>
                </TextBox.InputBindings>
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Grid>

        <!-- Button 2 -->
        <Grid Grid.Column="1" DataContext="{Binding Button2}">
            <!-- Display mode -->
            <Button Content="{Binding DisplayText}" Command="{Binding EditCommand}">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource ButtonBaseStyle}">
                        <Setter Property="Width" Value="104"/>
                        <Setter Property="Height" Value="24"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Edit mode -->
            <TextBox Text="{Binding EditValue, UpdateSourceTrigger=PropertyChanged}"
                     Foreground="{x:Static local:Theme.InteractiveTextColor}"
                     MaxLength="11"
                     LostFocus="HoldPointTextBox_LostFocus"
                     IsVisibleChanged="HoldPointTextBox_IsVisibleChanged">
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding CommitEditCommand}"/>
                    <KeyBinding Key="Escape" Command="{Binding CancelEditCommand}"/>
                </TextBox.InputBindings>
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Grid>

        <!-- Button 3 -->
        <Grid Grid.Column="2" DataContext="{Binding Button3}">
            <!-- Display mode -->
            <Button Content="{Binding DisplayText}" Command="{Binding EditCommand}">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource ButtonBaseStyle}">
                        <Setter Property="Width" Value="104"/>
                        <Setter Property="Height" Value="24"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Edit mode -->
            <TextBox Text="{Binding EditValue, UpdateSourceTrigger=PropertyChanged}"
                     Foreground="{x:Static local:Theme.InteractiveTextColor}"
                     MaxLength="11"
                     LostFocus="HoldPointTextBox_LostFocus"
                     IsVisibleChanged="HoldPointTextBox_IsVisibleChanged">
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding CommitEditCommand}"/>
                    <KeyBinding Key="Escape" Command="{Binding CancelEditCommand}"/>
                </TextBox.InputBindings>
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Grid>

        <!-- Button 4 -->
        <Grid Grid.Column="3" DataContext="{Binding Button4}">
            <!-- Display mode -->
            <Button Content="{Binding DisplayText}" Command="{Binding EditCommand}">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource ButtonBaseStyle}">
                        <Setter Property="Width" Value="104"/>
                        <Setter Property="Height" Value="24"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Edit mode -->
            <TextBox Text="{Binding EditValue, UpdateSourceTrigger=PropertyChanged}"
                     Foreground="{x:Static local:Theme.InteractiveTextColor}"
                     MaxLength="11"
                     LostFocus="HoldPointTextBox_LostFocus"
                     IsVisibleChanged="HoldPointTextBox_IsVisibleChanged">
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding CommitEditCommand}"/>
                    <KeyBinding Key="Escape" Command="{Binding CancelEditCommand}"/>
                </TextBox.InputBindings>
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Grid>
    </Grid>
</UserControl>
